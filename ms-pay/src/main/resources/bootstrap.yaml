spring:
  application:
    name: pay
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        # 共享配置
        shared-configs:
          - data-id: common.yaml
            refresh: true

          - data-id: pay.yaml
            refresh: true

          - data-id: rabbitmq.yaml
            refresh: true