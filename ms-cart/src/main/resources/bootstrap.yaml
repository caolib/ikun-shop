spring:
  application:
    name: cart
  cloud:
    nacos:
      server-addr: centos:8848
      config:
        shared-configs:
          - data-id: common.yaml
            refresh: true

          - data-id: cart.yaml
            refresh: true
