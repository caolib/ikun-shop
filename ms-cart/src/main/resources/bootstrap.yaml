spring:
  application:
    name: cart
  cloud:
    nacos:
      config:
        shared-configs:
          - data-id: common.yaml
            refresh: true

          - data-id: cart.yaml
            refresh: true
