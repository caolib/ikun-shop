spring:
  application:
    name: commodity
  cloud:
    nacos:
      config:
        shared-configs:
          - data-id: common.yaml
            refresh: true
          - data-id: commodity.yaml
            refresh: true