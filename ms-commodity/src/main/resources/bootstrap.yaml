spring:
  application:
    name: commodity
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        shared-configs:
          - data-id: common.yaml
            refresh: true
          - data-id: commodity.yaml
            refresh: true
