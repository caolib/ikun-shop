spring:
  application:
    name: commodity
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        shared-configs:
          - data-id: mysql-commodity.yaml
            refresh: true

          - data-id: mybatis.yaml
            refresh: true

          - data-id: redis.yaml
            refresh: true

          - data-id: logs.yaml
            refresh: true

          - data-id: openfeign.yaml
            refresh: true

          - data-id: sentinel.yaml
            refresh: true

          - data-id: seata.yaml
            refresh: true

          - data-id: status.yaml
            refresh: true
            group: DEV_GROUP